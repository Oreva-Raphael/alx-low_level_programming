#include <stdio.h>

/**
 * main - The main function
 *
 * Return: Always 0 (Successful)
 */
int main(void)
{
	int num = 10;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}

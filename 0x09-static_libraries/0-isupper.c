#include "main.h"
/*
 * _isupper - changing anything inputed from lower case to upper case
 * @c: the value to check if it is larger.
 * Return: return something other than 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}

#include "main.h"

/**
 * _strspn - something about strings i can't quite put my mind to it yet
 * @s: pointer to a character
 * @accept: pointer to something else
 * Return: probably going to rerurn something I don't know yet
 */
unsigned int _strspn(char *s, char *accept)
{

}

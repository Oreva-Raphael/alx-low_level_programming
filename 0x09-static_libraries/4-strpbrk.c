#include "main.h"

/**
 * _strpbrk - the function to do something I don't really know yet
 * @s: pointer to a character
 * @accept: pointer to something else but also a character
 * Return: returning something that isn't a number or integer
 */
char *_strpbrk(char *s, char *accept)
{

}

#include "main.h"

/**
 * _strstr - a function to maybe duplicate a string or repeat it or something
 * @haystack: a pointer to a character i don't know what for
 * @needle: a pointer to some other character I don't know what for
 * Return: return something that isn't an integer
 */
char *_strstr(char *haystack, char *needle)
{

}
